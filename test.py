import torch
from models.lenet import MnistNet4
from models.googlenet import GoogLeNet
import os
import re

# import numpy as np
# a = np.asarray([[1,2,3],[12,23,3]])
# b = [a,a,a,a]
# c =np.asarray(b)
# print(c.shape)
# print(a.shape)
# print(a.var(axis=1).mean(axis=0))
print([0] * 10)

